spring:
  application:
    name: personal-blogging-api

  datasource:
    url: jdbc:postgresql://localhost:5432/blogdb
    username: zh
    password: password

  jpa:
    hibernate:
      ddl-auto: update

    open-in-view: false